# Assets

Runtime assets committed with the Godot project.

Use `external_assets/` at the project root only for ignored local shared asset libraries. Do not symlink branch-owned assets wholesale across worktrees.

