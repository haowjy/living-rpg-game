# Story State

Event log, flags, quests, NPC memory, progression, and world consequences for the deterministic prototype.

This is not the LLM engine. Keep it runnable without model calls. Future LLM integrations can consume or propose changes through explicit data contracts.

